import styled from 'styled-components';

const CONTAINER_HEIGHT = '40vh';
const CONTAINER_MIN_HEIGHT = '350px';

export const Container = styled.div`
  height: ${CONTAINER_HEIGHT};
  min-height: ${CONTAINER_MIN_HEIGHT};
`;
